#include <stdio.h>
#include <string.h>

int main() {
    char str[100] = "hello";
    int len = strlen(str);

    // Swap characters from both ends
    for (int i = 0; i < len / 2; i++) {
        char temp = str[i];
        str[i] = str[len - i - 1];
